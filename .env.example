# PREFICTION Backend Configuration (Example)
# Copy this file to .env and update values

# ===== DATABASE =====
# MongoDB Atlas connection string
# Format: mongodb+srv://username:password@cluster.mongodb.net/database
MONGODB_URI=mongodb+srv://user:password@prefiction-cluster.mongodb.net/prefiction

# ===== SERVER =====
# Server port
PORT=3000

# Node environment
NODE_ENV=development

# ===== SECURITY =====
# Admin API Key (minimum 32 characters, mix of upper/lower/numbers/symbols)
# Generate: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
ADMIN_API_KEY=generate-a-secure-32-char-minimum-api-key-here

# Admin Panel Password (minimum 16 characters)
# Used for logging into /admin.html
ADMIN_PANEL_PASSWORD=your-secure-16-char-password-here

# ===== OPTIONAL =====
# Logging level
LOG_LEVEL=info

# CORS allowed origins (comma-separated)
# ALLOWED_ORIGINS=http://localhost:3001,https://yourdomain.com

# Session timeout in milliseconds (default: 1 hour = 3600000)
# SESSION_TIMEOUT=3600000

# ===== NOTES =====
# 1. Generate secure keys:
#    - API Key: 32+ random characters
#    - Password: 16+ mixed characters with numbers/symbols
#
# 2. MongoDB Atlas setup:
#    - Create cluster
#    - Create database user with strong password
#    - Whitelist your IP or use 0.0.0.0/0 (not recommended for prod)
#    - Copy connection string
#
# 3. .env file should be in .gitignore (never commit secrets)
#
# 4. For production, use strong passwords and API keys
#    Not the examples shown here!
